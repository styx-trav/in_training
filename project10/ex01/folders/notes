errno : refers to the int of the last known error

char *strerror(int errnum [assumedly the errno in question]) : translates from id number of error encountered into string explaining said error, ex. "Permission Denied."

cat concatenates files to standard output, including standard input as well
